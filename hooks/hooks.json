{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "YOUR_TEST_COMMAND_HERE"
          }
        ]
      },
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import sys,json,subprocess; d=json.load(sys.stdin); fp=d.get('tool_input',{}).get('file_path',''); (print('\\n=== SUPERVISED SCRATCHPAD UPDATE ==='), subprocess.run(['cat', fp]), print('=== END SUPERVISED SCRATCHPAD ===\\n')) if 'scratchpad/supervised' in fp else None\""
          }
        ]
      }
    ]
  }
}
